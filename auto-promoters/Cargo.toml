[package]
name = "auto-promoters"
version = "0.1.0"
edition = "2024"

[dependencies]
criterion = "0.8"
num = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
