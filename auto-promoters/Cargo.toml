[package]
name = "auto-promoters"
version = "0.1.0"
edition = "2024"

[dependencies]
criterion = "0.8"
num = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
bnum = { version = "0.14", features = ["serde", "numtraits"] }
malachite = { version = "0.9" }
# ibig, dashu, rug
